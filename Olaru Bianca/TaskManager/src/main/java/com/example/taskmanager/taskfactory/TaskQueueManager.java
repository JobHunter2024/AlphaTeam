package com.example.taskmanager.taskfactory;

import com.example.taskmanager.taskqueue.TaskQueue;

public class TaskQueueManager {

    public void addToQueue(TaskCommand taskCommand, TaskQueue taskQueue) {}

    public void getFromQueue(TaskCommand taskCommand, TaskQueue taskQueue) {}
}
